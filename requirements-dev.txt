# Development and verification toolchain.
#
# This file is part of the verification harness, not the application. Runtime
# dependencies belong in the package metadata created by the Phase 0 issue.
#
# Versions are pinned to a minor range so that a lint rule or type-checker
# change never turns a green branch red without a deliberate commit.

ruff~=0.15.0
mypy~=1.19.0
import-linter~=2.3
pytest~=9.0
pytest-qt~=4.4

# PySide6 is installable from PyPI and is enough for offscreen widget and
# view-model tests. Kirigami is a KDE Frameworks QML module and is NOT
# available from PyPI; QML that imports org.kde.kirigami can only be linted
# and exercised where the distribution packages are present. See AGENTS.md,
# "Verification tiers".
PySide6~=6.9
